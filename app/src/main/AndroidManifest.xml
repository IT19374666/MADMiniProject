<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MyAccount"
            android:label="@string/My_Acount_page"/>
        <activity
            android:name=".TransferMoneyFromAccount"
            android:label="@string/page_transfer_money"></activity>
        <activity
            android:name=".UpdateAccount"
            android:label="@string/page_update_account" />
        <activity
            android:name=".AddAccount"
            android:label="@string/page_add_account" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>